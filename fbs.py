# -*- coding: utf-8 -*-
import random,time,queue
from multiprogressing.managers import BaseManager

task_queue = queue.Queue();

result_queue = queue.Queue();

class QueueManager(BaseManager):
    pass
